local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
WindUI:Notify({Title = "B Hub", Content = "B hub Script Loaded Rally!", Duration = 3, Icon = "sparkles"})
local Window = WindUI:CreateWindow({Title = "B hub", Icon = "sparkles", Author = "by Snow", Folder = "BHubConfig", Size = UDim2.fromOffset(580, 460), Transparent = true, Theme = "Dark"})
local AimbotTab = Window:Tab({ Title = "Aimbot", Icon = "crosshair" })
local VisualsTab = Window:Tab({ Title = "Visuals & ESP", Icon = "eye" })
local PlayerTab = Window:Tab({ Title = "Player", Icon = "user" })
local SettingsTab = Window:Tab({ Title = "Settings", Icon = "settings" })
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")
local localPlayer = Players.LocalPlayer
local camera = Workspace.CurrentCamera
local fovRadius = 150
local fovYOffset = -30
local smoothSpeed = 0.1
local targetPartName = "Head"
local isAimbotEnabled = false
local isFovVisible = true
local isQuickAimVisible = false
local useTeamCheck = false
local useWallCheck = false
local fovColor = Color3.fromRGB(255, 255, 255)
local defaultWalkSpeed = 16
local defaultJumpPower = 50
local isSpeedEnabled = false
local isJumpEnabled = false
local customWalkSpeed = 16
local customJumpPower = 50
local isInfJumpEnabled = false
local isNoclipEnabled = false
local isAntiAfkEnabled = false
local isAntiFlingEnabled = false
local isAntiStunEnabled = false
local noclipConnection = nil
local antiAfkConnection = nil
local antiFlingConnection = nil
local antiStunConnection = nil
local isFlyEnabled = false
local flySpeed = 50
local bodyVelocity = nil
local bodyGyro = nil
local flyHeartbeatConnection = nil
local isNameEspEnabled = false
local isDistanceEspEnabled = false
local isTracersEnabled = false
local isRedChamsEnabled = false
local isFullbrightEnabled = false
local isNoFogEnabled = false
local defaultBrightness = Lighting.Brightness
local defaultClockTime = Lighting.ClockTime
local defaultFogEnd = Lighting.FogEnd
local isFpsPingVisible = false
local frameCount = 0
local lastFpsUpdate = tick()
local currentFps = 0
local tracersTable = {}
local currentConfigInputName = ""
local selectedConfigToLoad = "None"
local configDropdownWidget = nil
local aimbotToggleWidget = nil
local uiWidgets = {}
task.spawn(function()
    while true do
        task.wait(30)
        pcall(function() collectgarbage("collect") end)
    end
end)
local function startFlying(character)
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoidRootPart or not humanoid then return end
    humanoid.PlatformStand = true
    humanoid.AutoRotate = false
    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Name = "FlyVelocity"
    bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * 10^6
    bodyVelocity.Velocity = Vector3.zero
    bodyVelocity.Parent = humanoidRootPart
    bodyGyro = Instance.new("BodyGyro")
    bodyGyro.Name = "FlyGyro"
    bodyGyro.MaxTorque = Vector3.new(1, 1, 1) * 10^6
    bodyGyro.P = 10000
    bodyGyro.CFrame = camera.CFrame
    bodyGyro.Parent = humanoidRootPart
    flyHeartbeatConnection = RunService.Heartbeat:Connect(function()
        if not isFlyEnabled then return end
        local char = localPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum or not bodyVelocity or not bodyGyro then return end
        bodyGyro.CFrame = camera.CFrame
        local moveDir = hum.MoveDirection
        if moveDir.Magnitude > 0 then
            local camCFrame = camera.CFrame
            local lookVector = camCFrame.LookVector
            local rightVector = camCFrame.RightVector
            local flatLook = Vector3.new(lookVector.X, 0, lookVector.Z)
            if flatLook.Magnitude > 0 then flatLook = flatLook.Unit end
            local flatRight = Vector3.new(rightVector.X, 0, rightVector.Z)
            if flatRight.Magnitude > 0 then flatRight = flatRight.Unit end
            local forwardInput = moveDir:Dot(flatLook)
            local rightInput = moveDir:Dot(flatRight)
            local flyVector = (lookVector * forwardInput) + (rightVector * rightInput)
            if flyVector.Magnitude > 0 then flyVector = flyVector.Unit end
            bodyVelocity.Velocity = flyVector * flySpeed
        else
            bodyVelocity.Velocity = Vector3.zero
        end
    end)
end
local function stopFlying(character)
    if flyHeartbeatConnection then flyHeartbeatConnection:Disconnect() flyHeartbeatConnection = nil end
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if humanoid then humanoid.PlatformStand = false humanoid.AutoRotate = true end
    if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
    if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
end
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "UltimateAimbotUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = game:GetService("CoreGui")
local quickAimButton = Instance.new("TextButton")
quickAimButton.Name = "QuickAimButton"
quickAimButton.Size = UDim2.new(0, 75, 0, 75)
quickAimButton.Position = UDim2.new(1, -90, 0, 10)
quickAimButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
quickAimButton.TextColor3 = Color3.fromRGB(0, 0, 0)
quickAimButton.TextSize = 13
quickAimButton.Text = "Aim: OFF"
quickAimButton.Font = Enum.Font.SourceSansBold
quickAimButton.Active = true
quickAimButton.Visible = isQuickAimVisible
quickAimButton.Parent = screenGui
local quickAimCorner = Instance.new("UICorner")
quickAimCorner.CornerRadius = UDim.new(1, 0)
quickAimCorner.Parent = quickAimButton
local quickAimStroke = Instance.new("UIStroke")
quickAimStroke.Color = Color3.fromRGB(200, 200, 200)
quickAimStroke.Thickness = 2
quickAimStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
quickAimStroke.Parent = quickAimButton
local fpsPingLabel = Instance.new("TextLabel")
fpsPingLabel.Name = "FpsPingLabel"
fpsPingLabel.Size = UDim2.new(0, 180, 0, 28)
fpsPingLabel.Position = UDim2.new(0, 10, 0, 10)
fpsPingLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
fpsPingLabel.BackgroundTransparency = 0.2
fpsPingLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
fpsPingLabel.TextSize = 13
fpsPingLabel.Font = Enum.Font.SourceSansBold
fpsPingLabel.Text = "FPS: 0 | Ping: 0 ms"
fpsPingLabel.Visible = false
fpsPingLabel.Parent = screenGui
local fpsPingCorner = Instance.new("UICorner")
fpsPingCorner.CornerRadius = UDim.new(0, 6)
fpsPingCorner.Parent = fpsPingLabel
local fpsPingStroke = Instance.new("UIStroke")
fpsPingStroke.Color = Color3.fromRGB(255, 255, 255)
fpsPingStroke.Thickness = 1
fpsPingStroke.Transparency = 0.6
fpsPingStroke.Parent = fpsPingLabel
local fovCircle = Instance.new("Frame")
fovCircle.Name = "FOVCircle"
fovCircle.Size = UDim2.new(0, fovRadius * 2, 0, fovRadius * 2)
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.Position = UDim2.new(0.5, 0, 0.5, fovYOffset)
fovCircle.BackgroundTransparency = 1
fovCircle.Visible = isFovVisible
fovCircle.Parent = screenGui
local fovStroke = Instance.new("UIStroke")
fovStroke.Color = fovColor
fovStroke.Thickness = 1
fovStroke.Transparency = 0.5
fovStroke.Parent = fovCircle
local fovCorner = Instance.new("UICorner")
fovCorner.CornerRadius = UDim.new(1, 0)
fovCorner.Parent = fovCircle
local function getUniversalTargetPart(character)
    if not character then return nil end
    if targetPartName == "Head" then
        local head = character:FindFirstChild("Head") or character:FindFirstChild("head")
        if head and head:IsA("BasePart") then return head end
        for _, child in ipairs(character:GetDescendants()) do
            if child:IsA("BasePart") then
                local name = string.lower(child.Name)
                if string.find(name, "head") or string.find(name, "face") or string.find(name, "helmet") then return child end
            end
        end
    else
        local body = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso") or character:FindFirstChild("LowerTorso")
        if body and body:IsA("BasePart") then return body end
        for _, child in ipairs(character:GetDescendants()) do
            if child:IsA("BasePart") then
                local name = string.lower(child.Name)
                if string.find(name, "torso") or string.find(name, "chest") or string.find(name, "body") or string.find(name, "root") then return child end
            end
        end
    end
    return character:FindFirstChild("HumanoidRootPart") or character:FindFirstChildOfClass("BasePart")
end
local function isVisible(targetPart, character)
    local origin = camera.CFrame.Position
    local direction = (targetPart.Position - origin).Unit * (targetPart.Position - origin).Magnitude
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.FilterDescendantsInstances = {localPlayer.Character, character, camera}
    raycastParams.IgnoreWater = true
    return Workspace:Raycast(origin, direction, raycastParams) == nil
end
local function getClosestTargetInFOV()
    local character = localPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    local closestTarget = nil
    local shortestDistance = fovRadius
    local centerScreen = Vector2.new(camera.ViewportSize.X / 2, (camera.ViewportSize.Y / 2) + fovYOffset)
    local function checkValidTarget(object, playerOwner)
        if useTeamCheck and playerOwner and playerOwner.Team == localPlayer.Team then return end
        if object and object:FindFirstChild("Humanoid") and object.Humanoid.Health > 0 then
            local targetPart = getUniversalTargetPart(object)
            if targetPart then
                if useWallCheck and not isVisible(targetPart, object) then return end
                local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                if onScreen then
                    local distanceToCenter = (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude
                    if distanceToCenter < shortestDistance then
                        shortestDistance = distanceToCenter
                        closestTarget = targetPart
                    end
                end
            end
        end
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then checkValidTarget(player.Character, player) end
    end
    return closestTarget
end
local function updateAimbot()
    if not isAimbotEnabled then return end
    local targetPart = getClosestTargetInFOV()
    if targetPart then
        local currentCFrame = camera.CFrame
        local targetCFrame = CFrame.new(currentCFrame.Position, targetPart.Position)
        if smoothSpeed >= 1 then camera.CFrame = targetCFrame
        else camera.CFrame = currentCFrame:Lerp(targetCFrame, math.clamp(smoothSpeed, 0.01, 0.99)) end
    end
end
local function applyBillboardESP(player)
    if player == localPlayer then return end
    local function createGui(char)
        if not char then return end
        local head = getUniversalTargetPart(char)
        if not head then return end
        local existing = head:FindFirstChild("BHubESP")
        if existing then existing:Destroy() end
        local bgui = Instance.new("BillboardGui")
        bgui.Name = "BHubESP"
        bgui.Adornee = head
        bgui.Size = UDim2.new(0, 150, 0, 30)
        bgui.StudsOffset = Vector3.new(0, 2.5, 0)
        bgui.AlwaysOnTop = true
        bgui.Parent = head
        local textLabel = Instance.new("TextLabel")
        textLabel.Name = "ESPLabel"
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        textLabel.TextStrokeTransparency = 0
        textLabel.TextSize = 13
        textLabel.Font = Enum.Font.SourceSansBold
        textLabel.Parent = bgui
    end
    if player.Character then createGui(player.Character) end
    player.CharacterAdded:Connect(createGui)
end
for _, p in ipairs(Players:GetPlayers()) do applyBillboardESP(p) end
Players.PlayerAdded:Connect(applyBillboardESP)
Players.PlayerRemoving:Connect(function(player)
    if tracersTable[player] then
        pcall(function() tracersTable[player]:Remove() end)
        tracersTable[player] = nil
    end
end)
local function updateESP()
    local myChar = localPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    for player, line in pairs(tracersTable) do
        if not player or not player.Parent then
            pcall(function() line:Remove() end)
            tracersTable[player] = nil
        end
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local char = player.Character
            local line = tracersTable[player]
            if not char or not char.Parent or not char:FindFirstChild("HumanoidRootPart") then
                if line then line.Visible = false end
            else
                local head = getUniversalTargetPart(char)
                local hum = char:FindFirstChildOfClass("Humanoid")
                local bgui = head and head:FindFirstChild("BHubESP")
                if bgui and hum then
                    local label = bgui:FindFirstChild("ESPLabel")
                    local textParts = {}
                    if isNameEspEnabled then table.insert(textParts, player.Name) end
                    if isDistanceEspEnabled and myHRP and char:FindFirstChild("HumanoidRootPart") then
                        local dist = math.floor((char.HumanoidRootPart.Position - myHRP.Position).Magnitude)
                        table.insert(textParts, "[" .. dist .. "m]")
                    end
                    if label then
                        label.Text = table.concat(textParts, " ")
                        label.Visible = (isNameEspEnabled or isDistanceEspEnabled)
                    end
                end
                local highlight = char:FindFirstChild("RedChamsHighlight")
                if isRedChamsEnabled and hum and hum.Health > 0 then
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "RedChamsHighlight"
                        highlight.FillColor = Color3.fromRGB(255, 0, 0)
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.FillTransparency = 0.3
                        highlight.OutlineTransparency = 0
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.Adornee = char
                        highlight.Parent = char
                    end
                    highlight.Enabled = true
                else
                    if highlight then highlight.Enabled = false end
                end
                if Drawing then
                    if not line then
                        line = Drawing.new("Line")
                        line.Thickness = 1.5
                        line.Color = Color3.fromRGB(255, 50, 50)
                        line.Transparency = 0.8
                        tracersTable[player] = line
                    end
                    if isTracersEnabled and char:FindFirstChild("HumanoidRootPart") and hum and hum.Health > 0 then
                        local hrpPos, onScreen = camera:WorldToViewportPoint(char.HumanoidRootPart.Position)
                        if onScreen then
                            line.From = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y)
                            line.To = Vector2.new(hrpPos.X, hrpPos.Y)
                            line.Visible = true
                        else line.Visible = false end
                    else
                        if line then line.Visible = false end
                    end
                end
            end
        end
    end
end
local function setAimbotState(state)
    isAimbotEnabled = state
    quickAimButton.Text = isAimbotEnabled and "Aim: ON" or "Aim: OFF"
    quickAimStroke.Color = isAimbotEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(200, 200, 200)
    if aimbotToggleWidget and aimbotToggleWidget.Set then aimbotToggleWidget:Set(isAimbotEnabled) end
end
local function toggleAimbot() setAimbotState(not isAimbotEnabled) end
local function updateLighting()
    if isFullbrightEnabled then
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.GlobalShadows = false
    else
        Lighting.Brightness = defaultBrightness
        Lighting.ClockTime = defaultClockTime
        Lighting.GlobalShadows = true
    end
    Lighting.FogEnd = isNoFogEnabled and 100000 or defaultFogEnd
end
local function boostFps()
    Lighting.GlobalShadows = false
    Lighting.FogEnd = 9e9
    pcall(function() settings().Rendering.QualityLevel = 1 end)
    pcall(function()
        for _, v in ipairs(game:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") then v:Destroy()
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then v.Enabled = false end
        end
    end)
    WindUI:Notify({ Title = "B Hub", Content = "Boost FPS Success!", Duration = 3, Icon = "zap" })
end
local function getAllConfigs()
    local configs = {}
    if isfolder and isfolder("BHubConfig") then
        for _, file in ipairs(listfiles("BHubConfig")) do
            if string.sub(file, -5) == ".json" then
                local name = string.match(file, "BHubConfig[\\/](.+)%.json")
                if name then table.insert(configs, name) end
            end
        end
    end
    if #configs == 0 then table.insert(configs, "None") end
    return configs
end
local refreshConfigList
local function saveNamedConfig(name)
    if not name or name == "" then name = "1" end
    if makefolder and not isfolder("BHubConfig") then makefolder("BHubConfig") end
    local configData = {
        isAimbotEnabled = isAimbotEnabled, fovRadius = fovRadius, smoothSpeed = smoothSpeed, targetPartName = targetPartName,
        useTeamCheck = useTeamCheck, useWallCheck = useWallCheck, isFovVisible = isFovVisible, isQuickAimVisible = isQuickAimVisible,
        fovColor = { R = fovColor.R, G = fovColor.G, B = fovColor.B }, isRedChamsEnabled = isRedChamsEnabled, isNameEspEnabled = isNameEspEnabled,
        isDistanceEspEnabled = isDistanceEspEnabled, isTracersEnabled = isTracersEnabled, isFullbrightEnabled = isFullbrightEnabled,
        isNoFogEnabled = isNoFogEnabled, isSpeedEnabled = isSpeedEnabled, customWalkSpeed = customWalkSpeed, isJumpEnabled = isJumpEnabled,
        customJumpPower = customJumpPower, isInfJumpEnabled = isInfJumpEnabled, isNoclipEnabled = isNoclipEnabled, isAntiAfkEnabled = isAntiAfkEnabled,
        isAntiFlingEnabled = isAntiFlingEnabled, isAntiStunEnabled = isAntiStunEnabled, isFlyEnabled = isFlyEnabled, flySpeed = flySpeed
    }
    pcall(function() writefile("BHubConfig/" .. name .. ".json", HttpService:JSONEncode(configData)) end)
    WindUI:Notify({ Title = "B Hub", Content = "Config Saved: " .. name, Duration = 3, Icon = "save" })
    if refreshConfigList then refreshConfigList() end
end
